// Line 312-315: Replace with proper console.log
      // Publishing qualification step
      console.log('[ProjectQualificationStepSetup] Publishing qualification step:', {
        totalFields: Object.keys(cleanedData).length,
        fields: Object.keys(cleanedData)
      });